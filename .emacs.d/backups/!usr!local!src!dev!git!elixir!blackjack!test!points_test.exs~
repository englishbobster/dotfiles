defmodule PointsTest do
	use ExUnit.Case

	setup do
		{:ok, simple: [{:spades, :two}, {:spades, :seven}]}
	end

	test "that points can be calculated for a simple hand", context do
		assert points(context[:simple]) == 9
	end

end
