defmodule MyList do
  def span(from, to) do
    _span(from, to, [])
  end
  defp _span(current, to, result) when current == to do
    result
  end
  defp _span(current, to, result) do
    _span(current + 1, to, [current | result])
  end
end
