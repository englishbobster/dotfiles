defmodule Snimple.SnmpMessages do
  alias Snimple.SNMP.Header, as: Header
  alias Snimple.SnmpPdus, as: PDU

  def encode_snmpget(community, vblist, requid) do
    header = Header.encode_v2c_header(community)
    pdu = PDU.encode_pdu(vblist, requid, :snmpget)
    header <> pdu
  end

end
