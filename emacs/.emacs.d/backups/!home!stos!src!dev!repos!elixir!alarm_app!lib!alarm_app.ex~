defmodule AlarmApp do

  def main(_args) do
    {:ok, reply, _} = :snmpm.sync_get('oper', 'oper', [[1,3,6,1,2,1,1,1,0]])
    {:noError, _, list_o_stuff} = reply
    [h|_] = list_o_stuff
    li = Tuple.to_list(h)
    IO.puts("some result: #{Enum.at(li, 3)}")
  end

end
